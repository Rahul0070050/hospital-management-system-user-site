import { Symbol } from "typescript";

export interface couponData {
    firstName: String,
    lastName: String,
    email: String
}
export interface couponDataErroe {
    firstName: Boolean,
    lastName: Boolean,
    email: Boolean
}